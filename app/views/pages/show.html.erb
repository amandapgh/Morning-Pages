<h3><%= @page.title %></h3>
<p><%= @page.content %></p>
<p>Mood: <%= @page.mood %></p>

<%= button_to 'Destroy', page_path(@page),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
